Flask==3.1.3
flask-socketio==5.6.1
python-socketio==5.16.2
# WebSocket support for the threading async mode (falls back to long-polling without it)
simple-websocket==1.1.0
# Self-signed HTTPS certificates (launch.py and the app.py adhoc fallback)
cryptography>=42.0
